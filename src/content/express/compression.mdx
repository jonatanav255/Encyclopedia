export const level = 'senior';

# Compression

_Placeholder._ `compression` middleware (gzip/brotli), when not to
compress (already-compressed payloads, tiny responses), and why the
reverse proxy often does this instead.
