export const level = 'senior';

# Performance

_Placeholder._ Event-loop lag, profiling with `clinic.js` / `--prof`,
hot paths (JSON parsing, sync crypto, regex), and where a Node service
typically bottlenecks.
