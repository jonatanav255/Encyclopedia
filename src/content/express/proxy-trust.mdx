export const level = 'mid';

# Trust Proxy

_Placeholder._ `app.set('trust proxy', ...)`, what changes for
`req.ip` / `req.protocol` / `req.secure`, and why getting this wrong
breaks rate limiting and HTTPS detection behind a load balancer.
