export const level = 'mid';

# Async Handlers

_Placeholder._ Why a thrown error in an `async` route hangs the request
in Express 4, how Express 5 forwards rejections automatically, and the
`wrap(fn)` pattern when you can't upgrade.
