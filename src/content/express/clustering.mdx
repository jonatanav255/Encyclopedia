export const level = 'senior';

# Clustering

_Placeholder._ Node's `cluster` module / `pm2`, running multiple
processes per host, sticky sessions for stateful work, and why a
single Express process underutilizes a multi-core box.
