export const level = 'senior';

# Graceful Shutdown

_Placeholder._ Handling `SIGTERM`, calling `server.close()`,
draining in-flight requests, and the difference between graceful and
forced shutdown when orchestrators kill the process.
