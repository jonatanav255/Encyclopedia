export const level = 'mid';

# Auth Basics

_Placeholder._ Where the auth middleware sits in the chain, how to
attach `req.user`, session vs. JWT vs. API key, and what "logged in"
actually means at the protocol level.
