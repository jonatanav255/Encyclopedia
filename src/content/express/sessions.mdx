export const level = 'mid';

# Sessions

_Placeholder._ `express-session` basics, picking a store (memory is a
trap — Redis, Postgres, etc), the session-id cookie, and the
performance/cost tradeoff vs. stateless JWT.
