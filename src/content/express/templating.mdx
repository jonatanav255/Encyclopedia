export const level = 'mid';

# Templating

_Placeholder._ View engines (`ejs`, `pug`, `handlebars`),
`app.set('view engine', ...)`, `res.render`, and where SSR fits
between API-only Express and full-stack frameworks.
