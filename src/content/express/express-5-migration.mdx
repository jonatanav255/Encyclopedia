export const level = 'senior';

# Express 5 Migration

_Placeholder._ Breaking changes from 4 → 5: async error forwarding,
`req.params` becoming a plain object, route-pattern syntax changes
(no more `*` wildcard), and what `path-to-regexp` v8 means.
