export const level = 'mid';

# Testing

_Placeholder._ `supertest` against an Express `app` instance,
testing middleware in isolation, mocking vs. integration, and why
`app.listen()` doesn't have to run for tests.
