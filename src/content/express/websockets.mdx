export const level = 'senior';

# WebSockets

_Placeholder._ HTTP upgrade handshake, where `ws` and `socket.io` plug
into an Express server, sharing auth state with HTTP routes, and what
sticky routing requires.
