import type { QuestionBank } from './types';

export const bank: QuestionBank = {
  topic: 'design-principles',
  questions: [
    // --- junior ---
    {
      level: 'junior',
      question: 'What does SOLID stand for, in one line each?',
      answer:
        '**S — Single Responsibility.** A class should have one reason to change (one stakeholder, one axis of change), not one method.\n\n**O — Open/Closed.** Open for extension, closed for modification: add behavior without editing the existing module.\n\n**L — Liskov Substitution.** Subtypes must be usable wherever the base type is, *behaviorally* — not just by signature.\n\n**I — Interface Segregation.** Many small, role-shaped interfaces beat one fat interface that forces clients to depend on methods they don\'t use.\n\n**D — Dependency Inversion.** High-level policy depends on abstractions; low-level details implement them. The *direction* of source-code dependencies flips at the seam.\n\nThe acronym is a mnemonic, not a ranking. The five are independent heuristics for managing change in OO codebases — they don\'t compose into a procedure, and you can satisfy all five with an overengineered design.',
    },
    {
      level: 'junior',
      question: 'What does DRY actually mean — and why is "never type the same line twice" wrong?',
      answer:
        'The original Hunt & Thomas formulation: **every piece of *knowledge* must have a single, unambiguous, authoritative representation within a system.** The operative word is *knowledge*, not lines.\n\nTwo functions can look identical and still represent two different pieces of knowledge: `price * (1 - discount)` for pricing and `salary * (1 - taxRate)` for payroll have the same shape but different rules that evolve independently. Extracting `multiplyByOneMinus` couples them — now a pricing change has to consider payroll.\n\nReal DRY violations are when **the same fact** lives in two places: a regex for valid email written three times, tax brackets in code *and* a CSV, a column name duplicated in an ORM and a raw SQL query. Change one, forget the others, get bugs.\n\nSandi Metz\'s rule: **prefer duplication over the wrong abstraction.** Three near-copies are honest and easy to edit independently; a wrong abstraction lies about what the callers have in common, and "fixing" it ripples into callers that shouldn\'t care. The rule of three from *Refactoring* — wait until the third occurrence before extracting — is a guardrail against premature DRY.',
    },
    {
      level: 'junior',
      question: 'KISS — what does "simple" actually mean here?',
      answer:
        '"Keep It Simple" originated with Kelly Johnson at Lockheed Skunk Works: an aircraft design that an average mechanic can repair in the field beats a clever one that needs specialists. In software, the rule is **prefer the simplest design that solves the *actual* problem**.\n\nRich Hickey\'s distinction in *Simple Made Easy* sharpens it: **easy** is familiar/close-at-hand (subjective), **simple** is *unentangled* — few interleaved parts (objective). KISS aims at simple, not easy. A library you know well might be "easy" to use but drag many concepts into your code; a smaller tool might be "simple" but take a day to learn.\n\nSimplicity is measured in moving parts, concepts the reader holds at once, special cases, indirection layers, and dependencies. Every layer (a queue, a framework, a service boundary, a DI container) has a fixed tax: more failure modes, more to learn, more to keep patched, slower debugging. KISS pushes back when a layer doesn\'t earn that tax.\n\nA quick smell test: can you describe the design in one sentence? Could you delete a piece and still solve the problem? Are there configuration options nobody uses?',
    },
    {
      level: 'junior',
      question: 'Why is "favor composition over inheritance" the default advice?',
      answer:
        'GoF, 1994. Inheritance models *is-a* (a `Dog` is an `Animal`); composition models *has-a* (a `Car` has an `Engine`). When both are plausible, composition is more flexible with fewer surprises.\n\nFour known problems with deep inheritance:\n\n1. **Brittle base class** — changing a base method silently affects every subclass; the coupling is implicit.\n2. **Multi-axis modeling fails** — real domains vary along several independent axes (size, color, behavior). Single inheritance forces you to pick one for the hierarchy; the others need a different mechanism anyway.\n3. **Compile-time coupling** — a subclass is welded to its parent. You can\'t swap behaviors at runtime.\n4. **LSP hazards** — subclasses that "almost fit" the base break callers in non-obvious ways.\n\nComposition solves all four: the composed object exposes an interface (no implicit reach into internals), several independent components can sit side by side, the wiring is runtime-configurable, and the contract is the small composed interface, not the parent\'s full surface.\n\nThe classic refactor: replace a "type per behavior" hierarchy with a Strategy — pass behaviors in as components. The cost is more files and more wiring, paid back the first time you need a new combination.',
    },
    {
      level: 'junior',
      question: 'YAGNI — what is it and what does it specifically *not* argue against?',
      answer:
        '"You Aren\'t Gonna Need It." From Extreme Programming. Jeffries\' full form: **always implement things when you actually need them, never when you just foresee that you need them.**\n\nThe argument: foreseen needs are routinely wrong (estimates are 50%+ off on which features matter), and when the need *does* arrive it arrives in a different shape than you imagined, so the pre-built abstraction fits the wrong version. Meanwhile you pay carry cost on speculative code — surface area to read, more tests, more bugs in code that isn\'t battle-tested.\n\nWhat YAGNI does **not** argue against:\n\n- **Architectural decisions** that are expensive to reverse — database choice, authentication model, public API contracts — still need forethought. The retrofitting cost isn\'t "write the code now"; it\'s a production migration.\n- **Cross-cutting concerns** (observability, error handling, security). These are shapes the codebase takes, not features bolted on later.\n- **Leaving seams.** A function call where you *might* later add caching is cheap; the cache itself isn\'t. Seams are free.\n\nThe frame: **be conservative about adding code; be thoughtful about choosing shapes.** YAGNI also assumes you\'re willing to refactor; if change is expensive, the option-value of waiting drops.',
    },

    // --- mid ---
    {
      level: 'mid',
      question: 'Single Responsibility — what does "reason to change" actually mean?',
      answer:
        'Martin\'s motivating story: an `Employee` class has `calculatePay`, `reportHours`, and `save`. Three departments — Finance, HR, the DBA team — touch the same file. A Finance change to pay rules and an HR change to time-off can land on the same class on the same day, merge-conflict, and one team silently breaks the other. The class has **three reasons to change**, owned by three different stakeholders.\n\n"Reason to change" is shorthand for **a stakeholder or axis of evolution**. The sniff test: finish the sentence "this class changes when…" — if it takes multiple *and*-clauses joined by unrelated stakeholders, it\'s likely a violation.\n\nWhat SRP is *not*:\n\n- Not "one method per class." A `UrlParser` with `parse`, `parseQuery`, and `parseFragment` has one responsibility.\n- Not "small files." A 400-line class with one cohesive purpose is fine; a 40-line class mixing parsing and persistence isn\'t.\n- Not only for classes — modules and functions follow the same logic.\n\nThe pragmatic rule: **separate things that change for different reasons, owned by different people, on different schedules**. If all three of your "responsibilities" are owned by one team and always change together, the split is paperwork.',
    },
    {
      level: 'mid',
      question: 'Interface Segregation — header interfaces vs role interfaces.',
      answer:
        'Martin Fowler\'s framing makes ISP concrete:\n\n- **Header interface** — extracted from a class, mirrors its full public surface. Often fat. A `UserRepository` with 30 methods is the classic example.\n- **Role interface** — extracted from a *client*, contains only the methods that client uses. Small. `UserLookup` with one `findById`.\n\nISP says: prefer role interfaces. A class can implement many of them; each client depends only on its role. The benefit: changing a method on a role interface only touches the implementers and the clients that actually called it — not every consumer of the fat type.\n\nIn structurally typed languages (Go, TypeScript) this is almost free. Go\'s standard library is a tour of small role interfaces (`io.Reader`, `io.Writer`, `io.Closer`) composed at the call site. The function declares what it needs; anything with the right shape satisfies it. No `implements` keyword required.\n\nMark Seemann\'s sharper version: **let the consumer define the interface, not the provider.** That\'s the difference between a `UserRepository` you import (provider-defined) and a `UserLookup` you declare next to the code that needs it (consumer-defined). Smaller, more honest contracts, less recompilation when an unrelated method changes.\n\nWhen *not* to split: if two methods always travel together and you can\'t imagine a real implementer that has one without the other, the split is theoretical and probably premature.',
    },
    {
      level: 'mid',
      question: 'Separation of concerns: layered vs feature-sliced — what changes between them?',
      answer:
        'Two complementary axes for splitting code:\n\n- **Horizontal (layered)** — presentation / domain / persistence. Each layer depends only on the one below; cross-cutting concerns sit in middleware or decorators. Keeps infrastructure out of business logic.\n- **Vertical (feature-sliced)** — orders / billing / inventory. Each feature ships its own UI, API, and storage code together.\n\nThe failure mode of pure-horizontal: code for one feature is spread across `controllers/`, `services/`, `repos/`, `dtos/`. Adding "shipping label PDFs" touches eight folders. New contributors can\'t find anything by name.\n\nThe failure mode of pure-vertical: every feature reinvents transport, logging, retries. Cross-cutting concerns drift apart; logs from billing look nothing like logs from orders.\n\nThe combination that works in practice: **feature folders at the top level, layered concerns *inside* each feature**. Shared infrastructure (HTTP setup, the database client, logging) lives in a small `core/` or `infra/` directory that features import.\n\nFor cross-cutting concerns specifically: never sprinkle `log.info(...)` calls through every handler — use middleware/decorators so adding or removing logging is a one-place change. That\'s separation working at the right grain.',
    },
    {
      level: 'mid',
      question: 'Composition over inheritance — when is inheritance actually the right tool?',
      answer:
        '"Favor," not "never." Inheritance is defensible when several conditions hold together:\n\n- **The relationship is genuinely is-a in behavior**, not just structure. `LinkedHashMap` really is a `HashMap`; it adds ordering without changing the map contract. Penguin extending Bird-with-`fly()` isn\'t.\n- **The base is small, stable, and well-tested.** A short, public-surface base class is much safer to inherit from than a 2000-line one that evolves quarterly.\n- **The hierarchy is shallow.** One or two levels. Each level adds invisible coupling; three+ is where brittleness compounds.\n- **Subclasses honor LSP.** They pass the base\'s tests.\n- **The framework owns the contract.** Extending `React.Component`, `Activity`, or `unittest.TestCase` is fine — the framework defines and tests the contract you\'re fitting into.\n\nWhere inheritance fails predictably:\n\n- **Reuse-by-inheritance.** Extending a class just to grab utility methods.\n- **Deep hierarchies** across module boundaries you don\'t own.\n- **Multi-axis variation.** When the domain varies along several independent dimensions, single-inheritance forces a choice you\'ll regret.\n\nThe pattern most often used to convert "inheritance for behavior" to composition is **Strategy** — pass the behavior in as a component the class delegates to. Trades a subclass tree for a small interface plus injected implementations; gains runtime flexibility and testability.',
    },
    {
      level: 'mid',
      question: 'YAGNI versus thinking ahead — how do you balance them?',
      answer:
        'The pivot is **reversibility**. YAGNI is most defensible for decisions you can rip out and replace cheaply; less defensible for decisions you can\'t.\n\nThree categories:\n\n1. **Cheaply reversible code** — a function, a class, a route. Default to YAGNI. Build it when the second concrete need arrives (the first is information; the second is a pattern).\n2. **Architecture you can swap with effort** — your queue choice, your cache, your serialization format. Be thoughtful: pick something reasonable now, but don\'t over-invest in optionality you may not exercise. Adapters and seams help.\n3. **Decisions that lock in data or external contracts** — database engine, authentication scheme, public API shape, event schemas other teams consume. More upfront thought is warranted; "we\'ll change it later" means a production migration and possibly breaking other teams.\n\nThe practical tactics:\n\n- **Leave seams, not implementations.** A function call where caching *could* go later is cheap; the cache layer itself isn\'t.\n- **Decide late.** "Switch to Kafka when queue depth justifies it" is more defensible than "started with Kafka because we knew we\'d grow."\n- **Use the type system or a failing test as a memory aid** for "we\'ll need this when X happens" instead of building it.\n\nYAGNI also assumes refactor-friendliness. If your codebase is hostile to change, the answer is to fix that — not to stop using YAGNI, because losing the option to refactor turns every decision into a one-shot.',
    },

    // --- senior ---
    {
      level: 'senior',
      question: 'Walk through a Liskov Substitution violation that compiles cleanly.',
      answer:
        'The canonical: `Square extends Rectangle`. The compiler is happy because the signatures match; behavior is wrong.\n\n```ts\nclass Rectangle {\n  width: number; height: number;\n  setWidth(w: number)  { this.width  = w; }\n  setHeight(h: number) { this.height = h; }\n  area() { return this.width * this.height; }\n}\n\nclass Square extends Rectangle {\n  setWidth(w: number)  { this.width = w; this.height = w; }\n  setHeight(h: number) { this.width = h; this.height = h; }\n}\n\nfunction resize(r: Rectangle) {\n  r.setWidth(5); r.setHeight(4);\n  assert(r.area() === 20);   // holds for Rectangle, fails for Square\n}\n```\n\n`Square` passes the type checker but breaks an invariant the caller relied on: "setting width doesn\'t affect height." LSP violation.\n\nThe behavioral rules a subtype must respect (informal Liskov):\n\n- May **weaken** preconditions (accept more inputs).\n- May **strengthen** postconditions (return stricter guarantees).\n- Must **preserve** invariants of the base.\n- Must **not** throw new kinds of exceptions the base didn\'t declare.\n\nA subtype may do *more*, never *less*.\n\nOther common shapes: `ReadOnlyList extends List` that throws on `add()` (strengthening preconditions — Java\'s `Collections.unmodifiableList` family has this and it\'s documented as a known anti-pattern); `Penguin extends Bird` with `fly()` that throws; a `Stream` subclass whose `read()` returns nulls under conditions the base didn\'t allow.\n\nThe fix isn\'t "test for the subclass inside the caller" — that defeats polymorphism. The fix is interface segregation: `Readable` and `Writable` as separate interfaces; `ReadOnlyList` implements only `Readable`. The type system now refuses code that tries to `.add()` to a read-only list. Cheap LSP enforcement in practice: phrase the base\'s tests in terms of its interface and require subclasses to pass them — if they can\'t, the inheritance is wrong.',
    },
    {
      level: 'senior',
      question: 'Dependency Inversion vs dependency injection — what\'s the difference, and what makes it actually "inversion"?',
      answer:
        'They get conflated constantly. **DIP is a design shape**; **DI is a wiring technique**.\n\n- **Dependency Inversion (DIP)** — policy depends on abstractions; details implement them. The *direction* of `import` is what gets inverted.\n- **Dependency Injection (DI)** — passing a collaborator in (constructor / parameter / property) instead of constructing it internally.\n- **DI containers** — tools that automate wiring.\n\nYou can apply DIP without a container (just pass the interface in). You can use a container without applying DIP (resolve concrete classes).\n\nThe "inversion" itself: normally `checkout.ts` imports `smtp-client.ts` — high-level policy reaches into low-level mechanism. DIP flips the arrows. Both depend on an `EmailSender` interface. `checkout` imports `EmailSender`. `smtp-client` *also* imports `EmailSender` (to implement it). The detail now depends on the policy\'s contract.\n\nThe subtle requirement: **the abstraction is owned by the consumer (policy), not the provider (infrastructure)**. A common half-measure is to put the `EmailSender` interface inside the SMTP package; the policy still imports from infrastructure, just through an interface. That\'s not inversion — it\'s indirection. Full DIP: `EmailSender` lives in the domain package; the SMTP adapter package depends on the domain.\n\nThis is the shape behind **Hexagonal / Ports-and-Adapters** and **Clean Architecture**. The "port" is the consumer-owned interface; the "adapter" is the infrastructure that implements it.\n\nWhere DIP is overhead: an `IUuidGenerator` with one implementation, static utilities (`Math.max`), one-off scripts. A reliable test: would you actually swap this in a test, a different environment, or a different config? If not, the abstraction is theoretical.',
    },
    {
      level: 'senior',
      question: 'Open/Closed is the principle most often applied prematurely — when, why, and what\'s the cure?',
      answer:
        'OCP says: open for extension, closed for modification. The classic move is to replace a `switch` on type with a polymorphic interface + implementations, so new variants don\'t edit existing code.\n\nThe failure mode: **applying OCP at the first occurrence**, when you don\'t yet know what the axis of variation actually is.\n\nThe cost of a wrong extension point is worse than a switch statement:\n\n- An interface with one implementation is harder to read than a concrete class.\n- An interface shaped around the *wrong* axis forces you to refactor *on top of* the abstraction when the real variant arrives.\n- Implementers must satisfy methods that don\'t apply to them, producing throws/no-ops/lies.\n\nThe cure has two parts:\n\n1. **Sandi Metz: prefer duplication over the wrong abstraction.** Wait until the second or third variant appears; the variants tell you what the axis of variation actually is.\n2. **Use language features that give OCP\'s safety without the interface.** In TypeScript/Rust, a discriminated union with an exhaustive switch (`assertNever` / `_ => unreachable!()`) is a stable extension point — adding a variant is a compile error in *every* place you forgot to handle it. You get "no missed edits" without paying for an abstraction.\n\nThe deeper version: OCP isn\'t just about source not changing; it\'s about **the contract not changing**. A refactor that improves performance without changing return values doesn\'t violate OCP. A "fix" that quietly changes the meaning of `discount()` for existing callers does — extension point or not. That\'s the same property semver promises.\n\nWhen to break OCP deliberately: the cost of the abstraction exceeds the cost of editing (three places use a constant; just change the constant); the extension is one-off; the would-be variants don\'t share a meaningful interface.',
    },
    {
      level: 'senior',
      question: 'Law of Demeter — what it forbids, what it doesn\'t, and the design shape it produces.',
      answer:
        '"Only talk to your immediate friends." A method on `O` should only call methods on: `O` itself, `O`\'s fields, parameters, and objects `O` creates. What it forbids is **reaching through one object to call something on another**:\n\n```\norder.getCustomer().getAddress().getCity().toUpperCase()\n```\n\nThe depth of the chain is the depth of the coupling. Anything that changes in `Customer`, `Address`, or `City` can break `order`\'s caller. The Demeter-respecting version moves the operation to where the data lives: `order.shippingCityUpper()`. The chain still exists at runtime, but each step is between neighbors.\n\nWhat Demeter does **not** forbid:\n\n- **Fluent pipelines.** `rows.filter(active).map(byName).slice(0, 10)` is one type-lineage; Demeter is comfortable with chains along one axis.\n- **Plain data / DTOs.** A struct with public fields isn\'t an object in the Demeter sense; `record.user.name` is two field accesses, not delegated method calls. Wrapping it in `getUserName()` adds noise without removing coupling.\n\nThe complaint is specifically about chains across *objects that encapsulate behavior*.\n\nThe design shape Demeter produces:\n\n- Methods migrate to where the data lives.\n- Wrapper / facade methods proliferate (`order.shippingCity()` delegates).\n- Object graphs flatten — deep nesting becomes painful, so models restructure.\n\nA cheap test signal: if you build deep fake graphs to test something (`fake.customer.address.city`), the production code is reaching through layers. That\'s a Demeter signal.\n\nThe related slogan is **Tell, Don\'t Ask**: instead of pulling state out and acting on it externally, tell the object what to do (`user.zeroBalance(audit)` rather than reading the balance and writing it back). Demeter falls out naturally.',
    },
    {
      level: 'senior',
      question: 'SRP done wrong looks like an "anemic domain model." What\'s the trade-off?',
      answer:
        'Anemic domain model: classes that are just bags of fields with getters/setters; all behavior lives in *Service classes that operate on those bags. `User` has no methods of substance; `UserService.changeEmail(user, email)` does the work.\n\nThe over-application of SRP that produces this: "data and behavior are different responsibilities; separate them."\n\nFowler\'s critique: the anemic model is the inverse of what OO promises — you have the *shape* of objects (classes, fields) without the *encapsulation*. Logic that conceptually belongs to `User` is scattered across services; invariants the `User` should enforce are enforced (or not) at random call sites. Demeter violations multiply because callers reach into the bag to do anything useful.\n\nWhen anemic is *actually* fine:\n\n- **DTOs / transport objects.** Their job is to cross a boundary; they shouldn\'t have business logic.\n- **CQRS read models.** A view-shaped object designed for one query has no behavior on purpose.\n- **Functional cores in OO languages.** Plain records + pure functions is a valid style; it\'s "anemic" only if you expected behavior to live with data and it doesn\'t.\n\nWhen anemic is a smell:\n\n- The same invariant is reasserted in five `*Service` methods because no class owns it.\n- Callers manipulate `user.balance` directly; rules about negative balances live in three different handlers.\n- The "service" is a procedural function masquerading as a class.\n\nThe trade-off SRP is actually asking you to make: separate things that **change for different reasons**, not "data" from "behavior" by default. Behavior that always travels with the data should live on the class. Behavior that varies independently (export formats, transport, persistence) belongs elsewhere.',
    },

    {
      level: 'junior',
      question: 'Principle of Least Astonishment — what is it, and where does astonishment usually come from?',
      answer:
        'POLA, sometimes attributed to the PL/I committee in the 70s: **a component should behave in a way most users will expect**. The "users" include programmers calling your library, operators reading your logs, and end-users clicking your buttons.\n\nAstonishment isn\'t about behavior in isolation — it\'s about the *gap* between what someone expected and what happened. Two functions with identical behavior can have very different astonishment depending on naming and convention.\n\nThe usual sources:\n\n- **Names that don\'t match behavior** — `getUser()` triggering a network call, `parse()` writing to disk, `isValid()` having side effects. The most expensive thing the function does should be visible in its name.\n- **Hidden side effects** — a property getter that fetches and caches, a "config" function that mutates `process.env`.\n- **Surprising defaults** — `JSON.parse(\'null\')` returns `null`, `Number(\'\')` returns `0` not `NaN`.\n- **Local conventions that fight the language\'s** — an `async` function that returns a plain value, a class with `next()` that isn\'t an iterator.\n- **Inconsistency across siblings** — one endpoint returns `{ data: [...] }`, another returns `[...]`. One CLI flag is `--verbose`, the next is `-v`.\n\nThe practical test: **read your code\'s names without opening the bodies. Does the behavior match the promise?** If you\'d be surprised by what\'s inside, your callers will be too.\n\nThe principle is local to a user population: "expected by a Python programmer" can be "astonishing to a Haskell one." For public APIs, conform to idiomatic patterns in the host ecosystem; for internal code, follow the codebase\'s existing conventions even if you\'d personally have done it differently.',
    },
    {
      level: 'mid',
      question: 'Fail-fast, defensive, offensive — how do you decide which to apply where?',
      answer:
        'These aren\'t opposed if you locate them correctly: **defensive at the system boundary, offensive inside the boundary, fail-fast as the default failure mode in both**.\n\n- **Boundary code (defensive)** — HTTP handlers, public library functions, queue consumers, anything reading user input. You don\'t control the caller; treat input as hostile. Validate types/ranges/encodings, reject with a clear error, convert sentinels into typed values before passing inward.\n- **Interior code (offensive)** — functions called only by your own well-typed callers. Document preconditions; assert them; let the assertion crash *dev*. Skip "what if" branches the type system rules out. Don\'t re-validate what the boundary already validated.\n\nThe two failure modes to avoid:\n\n- **Only defensive everywhere.** Every function null-checks every argument; you\'ve reinvented dynamic typing on top of your static type system, paid in noise.\n- **Only offensive everywhere.** One bad request crashes a worker that was supposed to keep serving others.\n\nFail-fast is the *style* of failure inside both: detect violations as close to the cause as possible. Boundary code returns an error code on bad input; interior code throws/panics on impossible state. The shared enemy is the `try { … } catch (e) { log.error(e); }`-with-no-rethrow pattern — eats the signal, hides the bug, corrupts the system.\n\nThe **assertions vs guards** distinction is critical: assertions are for programmer errors and can be disabled in production (Rust `debug_assert!`, C `assert`); guards are for real-world input and must run always. Confusing them — using `panic!` on missing-but-optional config, or returning `null` from an "impossible" branch — is the most common version of this principle going wrong.\n\nThe sharper rule: **crash on programmer errors; handle user errors**. And remember Postel\'s law ("liberal in what you accept") built the early web *and* decades of security bugs — modern protocols (HTTP/2, HTTP/3) chose strict over forgiving for exactly that reason.',
    },

    // --- staff ---
    {
      level: 'staff',
      question: 'How do you teach a team to apply SOLID without producing overengineered code?',
      answer:
        'The failure mode you\'re trying to prevent: junior developers apply every letter to every class, producing 10 interfaces with one implementer each, factories for factories, and a 6-file feature that should have been one function. The principle becomes a ritual.\n\nA framing that works in practice:\n\n**1. Teach the *pain*, not the rule.** Each SOLID letter exists because of a concrete failure mode that you\'ve probably hit at scale. Tell the war story:\n\n- SRP — "Finance and HR landed conflicting changes on the same class on the same day."\n- OCP — "We added a new shape and forgot one of the five switches."\n- LSP — "The fake we used in tests passed; the subclass in prod failed silently because it strengthened preconditions."\n- ISP — "Every service rebuilt when we added one method to `UserRepository`."\n- DIP — "We couldn\'t test the checkout flow without standing up an SMTP server."\n\nIf people don\'t recognize the pain, the rule looks arbitrary and gets applied indiscriminately.\n\n**2. Apply with evidence, not preemption.** Sandi Metz\'s "prefer duplication over the wrong abstraction" generalizes: prefer concrete code over premature SOLID. Add the interface when you have a *second* implementation (not when you imagine one); split the class when two stakeholders actually fight over it; invert the dependency when the test pain is real.\n\n**3. Make the cost visible in review.** A pull request that adds an interface, a factory, and three implementations should answer: *what variant exists right now that justifies this?* If the answer is "none yet," push back.\n\n**4. Track the right outcome metric.** "Following SOLID" isn\'t a metric. What you can measure: how long the team takes to add the second variant of a feature; how often a change ripples into modules that shouldn\'t care; how often tests need real infrastructure. Those are the symptoms SOLID treats — if they\'re not present, the principle isn\'t earning its keep here.\n\n**5. Calibrate against language idioms.** SOLID was written for C++/Java in the 1990s. In TypeScript or Go, structural typing collapses ISP and DIP into near-default behavior; in Rust, traits + exhaustive matches give you OCP/LSP guarantees without classical inheritance; in functional code, "pass a function" is what OO calls Strategy + DIP. Don\'t pay Java\'s ceremony cost in languages that don\'t demand it.\n\nThe summary slogan: **SOLID is a diagnostic toolkit, not a checklist.** Use a letter when its symptom shows up; ignore it when it doesn\'t.',
    },
    {
      level: 'staff',
      question: 'When do these principles point in opposite directions, and how do you decide?',
      answer:
        'Several recurring tensions worth naming:\n\n**DRY vs SRP / coupling.** Two pieces of code with identical shape but different *reasons to change*. DRY says deduplicate; SRP says don\'t couple them. Resolve in favor of SRP — the duplication is only an apparent one. The shared abstraction will need flags or parameters within a year to handle each caller\'s drift.\n\n**DRY vs YAGNI.** Refactoring to remove duplication on the second occurrence. DRY-via-rule-of-three says wait for the third; YAGNI agrees. Don\'t deduplicate *yet*; wait for the variant that tells you what the abstraction should look like.\n\n**SOLID vs KISS.** Six interfaces with one implementer each, applied to a feature whose variation never materialized. The SOLID letters are heuristics for managing *change*; if the change axis is theoretical, KISS wins. Concrete code first; introduce the abstraction when a second implementation forces the issue.\n\n**OCP vs YAGNI.** Adding extension points for variants that don\'t exist. Wrong extension points are worse than no extension points — you refactor later *on top of* the abstraction. YAGNI wins. Leave a seam (a function boundary) instead of a typed extension point.\n\n**ISP vs cohesion.** Splitting an interface that always travels together produces noise without reducing coupling. Cohesion wins; only split for *independently varying* roles.\n\n**Composition over inheritance vs framework conventions.** Frameworks (React class components, JUnit `TestCase`, Android `Activity`) ask you to inherit. They own and test the contract; LSP is enforced by the framework\'s suite. Follow the framework; don\'t fight it.\n\n**Separation of concerns vs locality.** Excessively layered code disperses the logic of one feature across five layers, hurting comprehension. Feature folders at the top level + layered concerns inside each feature is the usual reconciliation.\n\n**LSP vs convenience.** A "ReadOnlyList extends List that throws on add" is convenient (one type, fits existing APIs) but a Liskov violation. Resolve by splitting interfaces (`Readable` / `Writable`) — *interface segregation buys you a place to put the read-only type honestly*.\n\nThe meta-principle: when two principles conflict, ask which **observed pain** each one is treating in this codebase. The principle whose symptom is actually present wins. Both can be ignored if neither symptom exists.',
    },
    {
      level: 'staff',
      question: 'How do these OO-flavored principles translate to functional or structurally typed codebases?',
      answer:
        'SOLID was crystallized for class-based OO (C++/Java/C# in the 90s/early 2000s). Most of it survives, but several letters compress or change shape in modern stacks.\n\n**Single Responsibility** — survives unchanged. The unit is now "module" or "function" instead of "class." The Martin definition (one reason to change, one stakeholder) maps cleanly. A module with one cohesive purpose; a function that does one job; that\'s SRP in any paradigm.\n\n**Open/Closed** — survives, but tooling shifts. In FP and structurally typed code, the OCP move isn\'t "introduce an interface and subclasses" — it\'s "pass a function" (Strategy) or "use a discriminated union with an exhaustive match." The Rust / TS discriminated union case is especially nice: a `switch` *is* a stable extension point, because adding a variant breaks every place you forgot to update at compile time. You get OCP\'s "no missed edits" without the interface.\n\n**Liskov Substitution** — survives, but mostly as a behavioral expectation. Without classical inheritance, you express subtyping through trait/interface implementations. Rust\'s trait system makes pure-structural LSP violations hard at the type level; the question becomes "does this `impl` actually meet the trait\'s documented contract," same content with less syntactic ceremony.\n\n**Interface Segregation** — *easier* in structurally typed languages. Go and TS satisfy interfaces by shape. A function declares the role interface it needs (`type Reader interface { Read(p []byte) (n int, err error) }`); any value with a `Read` method satisfies it. No `implements` keyword, no upfront declaration of every interface a class implements. The Go standard library is a tour of ISP done well — `io.Reader`, `io.Writer`, `io.Closer`, all small and composable.\n\n**Dependency Inversion** — also easier. In FP, "pass the dependency in" is just higher-order functions. The "abstraction" is a function type; the "implementation" is any function satisfying it. No interface declaration, no DI container. In Go, interfaces declared at the *consumer* (Seemann\'s framing) are idiomatic — the domain package declares `type EmailSender interface { ... }`; the adapter package\'s struct satisfies it implicitly.\n\n**Composition over inheritance** — trivially true in FP. There\'s no inheritance to prefer *against*; building behaviors by composing functions (`pipe(parse, validate, save)`) is the default. The principle becomes "don\'t pull in OO-style class hierarchies when functions and records will do."\n\n**Demeter** — survives. Reach-through coupling is bad in any paradigm. In FP, the equivalent is operating on nested record shapes by destructuring through layers; lenses/optics are the disciplined version.\n\nThe meta-translation: in modern languages, **a lot of SOLID gets paid by the type system instead of by ceremony**. The principles are still load-bearing, but the conventional Java/C# enactments (lots of interfaces, factories, DI containers) often look out of place in TS / Go / Rust / functional code. Apply the *intent*; let the language\'s native idioms carry it.',
    },
  ],
};
